# Kasus Nyata 2026-08-16 — Drift USB & Adopsi autoskills

## Drift Hermes USB (6 file) — resolusi penuh

Gejala: setelah upgrade sync ke seluruh folder + verifikasi hash, `--verify-target` Hermes USB lapor 6 `[ubah]`. Jcode & Hermes bersih.

Analisis: `stat` menunjukkan semua 6 file USB **lebih baru** dari bank → rsync -u benar (non-destruktif), modifikasi lokal USB dipertahankan; verifikasi hash mendeteksi drift (bekerja sesuai desain).

| File | Bank | USB | Keputusan |
|---|---|---|---|
| compliance-checklist-dashboard/SKILL.md | 281 baris | 321 (+40: Phase 6 indeks aktual, Mobile PWA) | backport USB → bank |
| document-content-pipeline/SKILL.md | 30.9 KB | 35.8 KB | backport |
| project-orientation/SKILL.md | 64 KB | 74 KB | backport |
| telegram-router-orchestration/SKILL.md | 17.8 KB | 30.4 KB | backport |
| pemdi-evidence-management/scripts/ocr_macos_vision.py | 1602 B | 1805 B (+catatan WAJIB venv Hermes/pyobjc) | backport |
| pemdi-evidence-management/SKILL.md | 628 baris | 41 baris | FORCE bank → USB (bank benar) |

Urutan eksekusi (dengan persetujuan user): 5× `cp USB → bank` + 1× `cp bank → USB` → regen manifest → sync → verify semua target LULUS → commit `66b7d53`.

Pelajaran: saat drift, selalu bandingkan konten + mtime, bukan hanya ukuran. File yang lebih besar di target bisa jadi LEBIH LENGKAP (perlu backport), bisa juga versi ringkas (bank benar). Cek diff sebelum memutuskan.

## Adopsi skill dari autoskills registry

Discovery: `npx -y autoskills --dry-run` di services/niu-mission-control mendeteksi Python/FastAPI/Flask + Frontend, merekomendasikan 11 skill, 3 fokus frontend:
- frontend-design (anthropics/skills, Apache-2.0, 42 baris — anti "AI slop")
- accessibility (addyosmani/web-quality-skills, MIT, 440 baris + 2 references — WCAG 2.2)
- seo (addyosmani/web-quality-skills, MIT) → SKIP (dashboard internal)

Diadopsi: accessibility + frontend-design → `skills/design/` dengan frontmatter bank (tambah `source:` + `license:` eksplisit), INDEX.md +2, manifest regen (42 skill, 255 file), sync semua target LULUS, commit `b456769`.

Konten registry lokal: clone midudev/autoskills → `packages/autoskills/skills-registry/<skill>/` + `index.json` (per-file SHA-256, bundleHash, review status approved/flagged). Semua skill di registry sudah di-audit prompt-injection oleh maintainer — aman diadopsi jika lisensi sesuai.

## Fix MC skill_monitor _get_home (commit ffb13c4)

Bug: `_get_home()` cek `os.path.isdir(home/Desktop/Niumination)` — folder KOSONG eksis di HOME cache Hermes → resolve ke `/Volumes/HermesAgent/.cache/unix-home` → `_scan_skill_bank()` scan path kosong → 40 skill di-flag "NOT in bank pusat" → 43 conflict palsu.

Fix: cek `Desktop/Niumination/skills` (folder berisi). Setelah fix: bank scan 40 → conflicts 43 → 0. Tes: `python3 -c "from modules.skill_monitor import _get_home, _scan_skill_bank; print(_get_home()); print(len(_scan_skill_bank(force=True)))"` → `/Users/zaryu`, 40.

Pola ini juga dipakai di sync-to-agents.sh `_REAL_HOME` dan skill-manifest.py `resolve_home()` — konsisten sejak awal. Hanya MC yang regress (cek induk) — jangan tiru pola lama.
