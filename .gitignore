# macOS
.DS_Store
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
Icon?

# Secrets & credentials
vault/
**/vault/
credentials-backup.zip
*.env
*.env.local
*.env.production
.env.*
secrets.zsh
api-key.md

# Dokumen pribadi (PII) — memuat NIK di nama file & inventaris dokumen pribadi.
# Repo ini PUBLIC: jangan di-commit. Salinan lokal tetap ada di tempatnya.
docs/reports/CLEANUP-PLAN-DOWNLOADS-DOCUMENTS-2026-09-16.md

# Sub-project directories (each has its own git repo)
apps/
services/
sites/
desktop/
agents/profile/
agents/orchestrator/
agents/ultra/
labs/
sandbox/

# Private dirs (each has own git repo or is local-only)
brain/
rekap/
tools/
dotfiles/

# Archive — historical backup, not tracked in root repo

# Build artifacts
node_modules/
.next/
dist/
build/
target/
.cache/
.venv/
__pycache__/
*.pyc
*.pyo

# IDE
.idea/
*.swp
*.swo
*~

# Binary files only
*.zip
*.gz
*.tar
*.png
*.jpg
*.jpeg
*.gif
*.ico

# Custom Finder folder icons (lokal — tidak di-track git)
.folder-icons/

# Sync artifacts (Layer 2)
.sync-lock/
.sync-log

.git-backup-*/
scripts/.trio-status.json
.9router-state.json

# Hermes runtime state
.hermes-memory/
# Artefak kerja agent (plan, draft) — salinannya yang final ada di docs/reports/
.hermes/

# Laporan audit keamanan — memuat peta kelemahan internal (repo yang bocor, alert
# yang belum dirotasi, isi rahasia, postur mesin). Repo ekosistem ini PUBLIK, jadi
# laporan sengaja disimpan lokal saja. Keputusan pemilik: 18 Sep 2026.
docs/reports/SECURITY-AUDIT-*.md

# Logs / runtime artifacts
logs/

# IDE local
.vscode/
skills-lock.json
archive/01-updates/
archive/backup/
archive/docs/
archive/media-lib/
archive/projects/
archive/projects-scan.json
archive/Belum disentuh/
archive/sapa-ai.bak/

# Repo dinas ASN (PRIVAT + sensitif) — diabaikan dari repo induk PUBLIK.
# Pola sama dengan labs/eKinerja-AfrizalMunthe (terverifikasi PRIVATE).
# Jangan pernah di-commit ke repo publik ini.
dinas/
